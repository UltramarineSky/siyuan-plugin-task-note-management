name: 🐛 错误报告
description: "报告软件中的错误或异常行为。"
title: "请输入Bug标题"
body:
  - type: checkboxes
    attributes:
      label: 是否已 Star 本仓库？
      description: 未 Star 的用户 Issue 会被直接关闭。
      options:
        - label: 我已 Star 本仓库
          required: true
  - type: checkboxes
    attributes:
      label: 是否已搜索现有 Issue？
      description: |
        请先在现有 Issue 中搜索，避免重复提交相同问题。
      options:
        - label: 我已搜索过现有 Issue
          required: true
  - type: checkboxes
    attributes:
      label: 此 Issue 是否只包含一个问题？
      description: |
        一个 Issue 只应报告一个 Bug。如有多个问题，请分别提交 Issue。
      options:
        - label: 此 Issue 只包含一个 Bug
          required: true
  - type: checkboxes
    attributes:
      label: 是否能在默认主题（白天/夜间）复现？
      description: |
        请确认该问题是否在默认主题下可复现，便于排查与主题相关的问题。
      options:
        - label: 我在默认主题下能复现该问题
          required: true
  - type: checkboxes
    attributes:
      label: 是否排除扩展影响？
      description: |
        主题、代码片段、插件等扩展可能导致问题，请尽量排除扩展影响后再提交问题。
      options:
        - label: 我已排除扩展导致问题的可能性
          required: true
  - type: textarea
    attributes:
      label: 问题描述（必须）
      description: |
        请尽可能清晰地描述遇到的问题、出现频率及出现时间点。
    validations:
      required: true
  - type: textarea
    attributes:
      label: 期望结果（必须）
      description: |
        请清楚描述你期望得到的正确行为或结果。
    validations:
      required: true
  - type: textarea
    attributes:
      label: 截图或录像（可选）
      description: |
        请尽量附上截图或录屏，能更快定位问题。
    validations:
      required: false
  - type: textarea
    attributes:
      label: 版本与环境（必须）
      value: |
        - 插件版本：
        - 思源笔记版本：
        - 操作系统：
        - 浏览器（若使用）：
      render: markdown
    validations:
      required: true
  - type: textarea
    attributes:
      label: 其他信息（可选）
      description: |
        如有其他可帮助诊断的信息（网络状况、重现步骤、特殊配置等），请一并提供。
    validations:
      required: false
